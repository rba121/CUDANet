•	Built a neural network framework from scratch with custom GPU kernels for forward and backward propagation, achieving a ~40% reduction in training step time versus a CPU baseline.
•	Optimized CUDA kernels for matrix operations by tuning grid/block configurations and memory access patterns; implemented core layers and an end-to-end training pipeline.
